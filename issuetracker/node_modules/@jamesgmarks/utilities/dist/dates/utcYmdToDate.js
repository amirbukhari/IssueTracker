"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const intVal_1 = require("../misc/intVal");
/**
 *
 * @param str A date string expected to be in the format `[year]-[month]-[day]T[hour]:[min]:[sec]:[ms]` in 24 hour UTC time
 *  - you may omit the entire timestamp, just don't include the T
 */
exports.utcYmdToDate = (str) => {
    const [date, time] = str.split('T');
    const [year, month, dayOfMonth] = date.split('-').map((s) => intVal_1.intVal(s));
    const [hour, minute, second, millisecond] = (time !== null && time !== void 0 ? time : '').split(':').filter((s) => s !== '').map((s) => intVal_1.intVal(s));
    if (hour) {
        if (millisecond)
            return new Date(Date.UTC(year, month - 1, dayOfMonth, hour, minute, second, millisecond));
        return new Date(Date.UTC(year, month - 1, dayOfMonth, hour, minute, second));
    }
    return new Date(Date.UTC(year, month - 1, dayOfMonth));
};
//# sourceMappingURL=utcYmdToDate.js.map