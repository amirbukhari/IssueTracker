/**
 * Logical NOT operation
 * @param {boolean} x
 */
export declare const not: (x: boolean) => boolean;
/**
 * Logical AND operation. Both values must be truthy
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const and: (a: boolean, b: boolean) => boolean;
/**
 * Logical NAND operation. At least one value is not truthy.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const nand: (a: boolean, b: boolean) => boolean;
/**
 * Logical OR operation. At least one value is truthy.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const or: (a: boolean, b: boolean) => boolean;
/**
 * Logical NOR operation. Neither value is truthy.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const nor: (a: boolean, b: boolean) => boolean;
/**
 * Logical XOR operation. One and only one value is truthy.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const xor: (a: boolean, b: boolean) => boolean;
/**
 * Strict logical NOT operation If all operands are not boolean type an error will be thrown.
 * @param {boolean} x
 */
export declare const strictNot: (x: boolean) => boolean;
/**
 * Strict logical AND operation. Both values must be truthy If all operands are not boolean type an error will be thrown.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const strictAnd: (a: boolean, b: boolean) => boolean;
/**
 * Strict logical NAND operation. At least one value is not truthy. If all operands are not boolean type an error will be thrown.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const strictNand: (a: boolean, b: boolean) => boolean;
/**
 * Strict logical OR operation. At least one value is truthy. If all operands are not boolean type an error will be thrown.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const strictOr: (a: boolean, b: boolean) => boolean;
/**
 * Strict logical NOR operation. Neither value is truthy. If all operands are not boolean type an error will be thrown.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const strictNor: (a: boolean, b: boolean) => boolean;
/**
 * Strict logical XOR operation. One and only one value is truthy. If all operands are not boolean type an error will be thrown.
 * @param {boolean} a
 * @param {boolean} b
 */
export declare const strictXor: (a: boolean, b: boolean) => boolean;
//# sourceMappingURL=logical.d.ts.map