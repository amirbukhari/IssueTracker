"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const isPrimitive_1 = require("../misc/isPrimitive");
const deepClone_1 = require("../objects/deepClone");
/**
 * Clones `array` ensuring reference types are copied by value and not by reference.
 *
 * ! Warning: This could result in infinite recursion if circular references exist inside the object.
 *
 * @param array Array to clone
 */
exports.deepCloneArray = (array) => array.map((x) => (isPrimitive_1.isPrimitive(x) // eslint-disable-line no-nested-ternary
    ? x
    : (Array.isArray(x) ? exports.deepCloneArray(x) : deepClone_1.deepClone(x))));
//# sourceMappingURL=deepCloneArray.js.map