"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const reduce_1 = require("./reduce");
const hasAny_1 = require("./hasAny");
const identical_1 = require("../objects/identical");
/**
 * Helper method: Returns an array consisting only of distinct values. Intended for arrays containing a set of
 * objects, which may have different reference values, but have the potential to contain repeat data.
 * @param array - array to filter
 */
exports.distinctObjects = (array) => reduce_1.reduce(array, (acc, el) => {
    const alreadyProcessed = hasAny_1.hasAny(acc, (curr) => identical_1.identical(curr, el));
    return alreadyProcessed ? [...acc] : [...acc, el];
}, []);
//# sourceMappingURL=distinctObjects.js.map