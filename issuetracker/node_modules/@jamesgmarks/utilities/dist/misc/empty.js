"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const undef_1 = require("./undef");
const isNull_1 = require("./isNull");
const count_1 = require("../arrays/count");
const isArray_1 = require("../arrays/isArray");
const isDefinedObject_1 = require("../objects/isDefinedObject");
const keys_1 = require("../objects/keys");
/**
 * Closely equivalent to PHP's rules for empty().
 *  - Note: Treats both `undefined` and `null` as PHP's `null` for this method.
 * Returns true if x is undefined, null, 0, '' (empty string), '0', false, an empty array or an object with no keys.
 * */
exports.empty = (x) => (undef_1.undef(x) || isNull_1.isNull(x) || x === 0 || x === '' || x === '0' || x === false
    || (isArray_1.isArray(x) && count_1.count(x) === 0) || (isDefinedObject_1.isDefinedObject(x) && count_1.count(keys_1.keys(x)) === 0));
//# sourceMappingURL=empty.js.map